# 会员系统接入文档（Membership）

> 适用于需要会员订阅功能的项目（如 Girls）
> 
> 版本：v1.0  
> 更新日期：2025-12-20

---

## 📋 目录

- [系统概述](#系统概述)
- [会员体系设计](#会员体系设计)
- [前端类型定义](#前端类型定义)
- [接入步骤](#接入步骤)
- [API 接口](#api-接口)
- [使用示例](#使用示例)
- [常见问题](#常见问题)

---

## 系统概述

### 核心功能

- ✅ 会员购买（支持支付宝/微信支付）
- ✅ 自动开通会员权限
- ✅ 续费期限自动叠加
- ✅ 终身会员支持（100年有效期）
- ✅ 会员信息 JWT 自动下发

### 技术架构

```
前端下单 → 通用支付接口 → 创建订单 → 第三方支付
                                    ↓
用户信息更新 ← Hook触发 ← 支付成功回调
```

---

## 会员体系设计

### 推荐方案：两级会员体系

| 会员类型 | level 值 | 到期时间 | 价格建议 | 说明 |
|---------|---------|---------|---------|------|
| 免费用户 | `free` | - | ¥0 | 默认状态，无需任何字段 |
| 终身会员 | `lifetime` | 100年后 | ¥299 | 一次购买永久有效 |

### 其他可选方案

根据项目需求，可以扩展更多等级：

```javascript
const MEMBERSHIP_LEVELS = {
  free: '免费用户',       // 默认
  monthly: '月度会员',    // 30天
  yearly: '年度会员',     // 365天
  lifetime: '终身会员'    // 1200个月（100年）
};
```

---

## 前端类型定义

### TypeScript 类型

```typescript
/**
 * Girls 项目 - 会员系统类型定义
 */

// 会员等级枚举
export enum MembershipLevel {
  Free = 'free',       // 免费用户
  Lifetime = 'lifetime' // 终身会员
}

// 用户会员信息（从 JWT 登录响应中获取）
export interface UserMembership {
  membership_level?: MembershipLevel;  // 会员等级
  membership_expire?: string;          // 到期时间 ISO 8601 (e.g. "2125-12-20 14:30:00")
}

// JWT 登录响应
export interface LoginResponse {
  token: string;                       // JWT token
  user_email: string;                  // 用户邮箱
  user_nicename: string;               // 用户昵称
  user_display_name: string;           // 显示名称
  user_id: number;                     // 用户ID
  user_roles: Record<string, boolean>; // 用户角色
  membership_level?: MembershipLevel;  // 会员等级
  membership_expire?: string;          // 会员到期时间
}

// 会员购买参数
export interface MembershipPurchaseParams {
  type: 'membership';                  // 固定值
  amount: number;                      // 金额（分）
  method: 'alipay' | 'wechat';        // 支付方式
  device: 'wap' | 'web' | 'app';      // 设备类型
  duration_months: number;             // 有效期（月），终身会员建议 1200
  level?: MembershipLevel;             // 会员等级，默认 "basic"
  title?: string;                      // 订单标题，默认自动生成
}

// 会员状态检查工具类
export class MembershipHelper {
  /**
   * 检查用户是否是会员
   */
  static isMember(user: UserMembership): boolean {
    if (!user.membership_expire) return false;
    
    const expireTime = new Date(user.membership_expire).getTime();
    const now = Date.now();
    
    return expireTime > now;
  }

  /**
   * 检查用户是否是终身会员
   */
  static isLifetimeMember(user: UserMembership): boolean {
    return user.membership_level === MembershipLevel.Lifetime && this.isMember(user);
  }

  /**
   * 获取会员剩余天数
   */
  static getRemainingDays(user: UserMembership): number {
    if (!user.membership_expire) return 0;
    
    const expireTime = new Date(user.membership_expire).getTime();
    const now = Date.now();
    const remainingMs = expireTime - now;
    
    return Math.max(0, Math.ceil(remainingMs / (1000 * 60 * 60 * 24)));
  }

  /**
   * 格式化到期时间显示
   */
  static formatExpireDate(user: UserMembership): string {
    if (!this.isMember(user)) return '未开通';
    
    if (this.isLifetimeMember(user)) {
      return '终身会员';
    }
    
    const days = this.getRemainingDays(user);
    if (days > 365) {
      return `${Math.floor(days / 365)}年后到期`;
    }
    
    return `${days}天后到期`;
  }
}
```

---

## 接入步骤

### 步骤 1: 配置项目环境

确保项目已启用 JWT 认证插件：`wp-api-jwt-auth`

### 步骤 2: 配置会员套餐

```typescript
// Girls 项目会员套餐配置
export const MEMBERSHIP_PLANS = {
  lifetime: {
    id: 'lifetime',
    name: 'Girls 终身会员',
    price: 29900,              // 299元
    duration_months: 1200,     // 100年
    level: MembershipLevel.Lifetime,
    features: [
      '无限次观看所有内容',
      '专属会员标识',
      '优先客服支持',
      '永久有效'
    ]
  }
};
```

### 步骤 3: 实现购买流程

```typescript
async function purchaseMembership(planId: string) {
  const plan = MEMBERSHIP_PLANS[planId];
  if (!plan) throw new Error('Invalid plan');

  // 1. 创建订单并发起支付
  const response = await fetch('https://api.girls.chuchuang.work/wp/v2/payment/create', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${getJwtToken()}`  // JWT 认证
    },
    body: JSON.stringify({
      type: 'membership',
      amount: plan.price,
      method: 'wechat',         // 或 'alipay'
      device: 'wap',
      duration_months: plan.duration_months,
      level: plan.level,
      title: plan.name
    })
  });

  const result = await response.json();

  if (result.status) {
    // 2. 跳转到支付页面
    window.location.href = result.data.pay_url;
  } else {
    throw new Error(result.msg);
  }
}
```

### 步骤 4: 支付回调处理

支付成功后，系统自动触发：
- Hook: `payment_success_membership`
- 自动更新用户的 `membership_level` 和 `membership_expire`
- 前端无需额外处理，用户重新登录即可获取最新会员状态

### 步骤 5: 前端权限判断

```typescript
// 登录获取用户信息
const user: LoginResponse = await login(email, password);

// 判断会员状态
if (MembershipHelper.isLifetimeMember(user)) {
  // 显示会员专属内容
  showMemberContent();
} else if (MembershipHelper.isMember(user)) {
  // 普通会员
  const days = MembershipHelper.getRemainingDays(user);
  console.log(`会员剩余 ${days} 天`);
} else {
  // 免费用户，提示购买
  showPurchaseDialog();
}
```

---

## API 接口

### 1. 用户登录（获取会员信息）

**接口**: `POST /wp-json/jwt-auth/v1/token`

**请求参数**:
```json
{
  "username": "user@example.com",
  "password": "password123"
}
```

**响应示例**:
```json
{
  "token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "user_email": "user@example.com",
  "user_nicename": "张三",
  "user_display_name": "张三",
  "user_id": 123,
  "user_roles": {
    "subscriber": true
  },
  "membership_level": "lifetime",
  "membership_expire": "2125-12-20 14:30:00"
}
```

### 2. 创建会员订单

**接口**: `POST /wp/v2/payment/create`

**请求头**:
```
Authorization: Bearer {jwt_token}
Content-Type: application/json
```

**请求参数**:
```json
{
  "type": "membership",
  "amount": 29900,
  "method": "wechat",
  "device": "wap",
  "duration_months": 1200,
  "level": "lifetime",
  "title": "Girls终身会员"
}
```

**响应示例**:
```json
{
  "status": true,
  "msg": "success",
  "data": {
    "pay_url": "https://...",
    "order_id": 12345,
    "out_trade_no": "202512200631520013673"
  }
}
```

### 3. 查询订单支付结果

**接口**: `POST /wp/v2/payment/find`

**请求参数**:
```json
{
  "method": "wechat",           // 必填：支付方式 (alipay/wechat)
  "out_trade_no": "202512..."   // 必填：订单号
}
```

**响应示例**:
```json
{
  "status": true,
  "msg": "success",
  "data": {
    "trade_status": "TRADE_SUCCESS",
    "amount": 29900,
    "paid_at": "2025-12-20 14:30:00"
  }
}
```

---

## 使用示例

### 完整购买流程

```typescript
import { MembershipLevel, MembershipHelper, MEMBERSHIP_PLANS } from './types';

class MembershipService {
  private apiBase = 'https://api.girls.chuchuang.work';
  private jwtToken: string;

  constructor(token: string) {
    this.jwtToken = token;
  }

  /**
   * 购买会员
   */
  async purchase(planId: string): Promise<void> {
    const plan = MEMBERSHIP_PLANS[planId];
    if (!plan) throw new Error('套餐不存在');

    try {
      const response = await fetch(`${this.apiBase}/wp/v2/payment/create`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Authorization': `Bearer ${this.jwtToken}`
        },
        body: JSON.stringify({
          type: 'membership',
          amount: plan.price,
          method: 'wechat',
          device: 'wap',
          duration_months: plan.duration_months,
          level: plan.level,
          title: plan.name
        })
      });

      const result = await response.json();

      if (result.status) {
        // 跳转支付
        window.location.href = result.data.pay_url;
      } else {
        throw new Error(result.msg);
      }
    } catch (error) {
      console.error('购买失败:', error);
      throw error;
    }
  }

  /**
   * 检查订单支付状态
   */
  async checkPaymentStatus(outTradeNo: string, method: string): Promise<boolean> {
    const response = await fetch(
      `${this.apiBase}/wp/v2/payment/find`,
      {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Authorization': `Bearer ${this.jwtToken}`
        },
        body: JSON.stringify({
          method: method,
          out_trade_no: outTradeNo
        })
      }
    );

    const result = await response.json();
    return result.status && result.data.trade_status === 'TRADE_SUCCESS';
  }
}

// 使用示例
async function handlePurchase() {
  const jwtToken = localStorage.getItem('jwt_token');
  const service = new MembershipService(jwtToken);

  try {
    // 购买终身会员
    await service.purchase('lifetime');
  } catch (error) {
    alert('购买失败，请重试');
  }
}

// 支付回调页面
async function handlePaymentCallback() {
  const urlParams = new URLSearchParams(window.location.search);
  const outTradeNo = urlParams.get('out_trade_no');
  const method = urlParams.get('method') || 'wechat';  // 从 URL 获取支付方式

  if (outTradeNo) {
    const service = new MembershipService(localStorage.getItem('jwt_token'));
    const isPaid = await service.checkPaymentStatus(outTradeNo, method);

    if (isPaid) {
      // 支付成功，重新登录获取最新会员信息
      await reLogin();
      showSuccessMessage('恭喜您成为终身会员！');
    } else {
      showErrorMessage('支付未完成或已取消');
    }
  }
}
```

### 会员状态展示组件

```typescript
// React 组件示例
import React from 'react';
import { useAuth } from './hooks/useAuth';
import { MembershipHelper } from './types';

export const MembershipBadge: React.FC = () => {
  const { user } = useAuth();

  if (!user) return null;

  const isMember = MembershipHelper.isMember(user);
  const isLifetime = MembershipHelper.isLifetimeMember(user);
  const expireText = MembershipHelper.formatExpireDate(user);

  return (
    <div className="membership-badge">
      {isLifetime ? (
        <span className="badge-lifetime">👑 终身会员</span>
      ) : isMember ? (
        <span className="badge-active">⭐ 会员 - {expireText}</span>
      ) : (
        <button onClick={handlePurchase}>
          开通会员
        </button>
      )}
    </div>
  );
};
```

---

## 常见问题

### Q1: 终身会员的有效期是多久？

**A**: 100年（1200个月），在系统中视为永久有效。

### Q2: 续费会覆盖原有期限吗？

**A**: 不会。续费会在原有到期时间基础上叠加。例如：
- 当前到期时间：2026-12-20
- 购买1年会员
- 新到期时间：2027-12-20

### Q3: 如何判断用户是否是会员？

**A**: 使用 `MembershipHelper.isMember(user)` 方法，它会自动判断 `membership_expire` 是否过期。

### Q4: 会员信息什么时候更新？

**A**: 用户每次登录（调用 JWT `/token` 接口）时，会自动获取最新的会员状态。

### Q5: 支持哪些支付方式？

**A**: 目前支持：
- ✅ 支付宝（H5/Web/App）
- ✅ 微信支付（H5/小程序/App）

### Q6: Girls 项目只有终身会员，需要传 level 参数吗？

**A**: 建议传递 `level: "lifetime"`，便于：
- 前端直接判断会员类型
- 后台数据统计和分析
- 未来扩展其他会员等级

### Q7: 如果不传 title 会怎样？

**A**: 系统会自动生成默认标题 `"Membership订单"`，但不如自定义标题友好。建议传递 `title: "Girls终身会员"` 提升用户体验。

### Q8: 需要实现会员专属接口吗？

**A**: 不需要。在需要会员权限的接口中，直接判断用户的 `membership_expire` 即可：

```php
// PHP 后端示例
$user_id = get_current_user_id();
$expire = get_user_meta($user_id, 'membership_expire', true);

if (!$expire || strtotime($expire) < time()) {
    return new WP_Error('not_member', '仅限会员访问', ['status' => 403]);
}
```

---

## 附录

### Girls 项目推荐配置

```typescript
// 会员套餐
export const GIRLS_MEMBERSHIP = {
  lifetime: {
    id: 'lifetime',
    name: 'Girls 终身会员',
    price: 29900,              // ¥299
    originalPrice: 59900,      // ¥599（原价）
    duration_months: 1200,     // 100年
    level: 'lifetime',
    features: [
      '永久解锁所有内容',
      '专属会员身份标识',
      '优先观看最新内容',
      '专属客服通道'
    ]
  }
};

// API 配置
export const API_CONFIG = {
  baseURL: 'https://api.girls.chuchuang.work',
  endpoints: {
    login: '/wp-json/jwt-auth/v1/token',
    payment: '/wp/v2/payment/create',
    paymentQuery: '/wp/v2/payment/find'
  }
};
```

---

## 技术支持

如有问题，请联系技术团队或查看：
- [前端支付接入文档](./前端支付接入文档.md)
- [订单系统技术文档](./打赏流程设计.md)

---

**文档版本**: v1.0  
**最后更新**: 2025-12-20  
**适用项目**: Girls, Dating, Work（所有需要会员功能的项目）
